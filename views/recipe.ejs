<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="/styles.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <title>Bread Recipes</title>
</head>
<body>
    <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
        <div class="container-fluid">
            <a class="navbar-brand" href="/">Bread Recipes</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="collapsibleNavbar">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link" href="/addBuns">Add new bread</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>
    <div class="container">
        <br>
        <div class="text-center"><h2>Bread Recipe Details</h2></div>
        <br>
        <% if (buns) { %>
            <div>
                <p><b>Bread Name:</b><br> <%= buns.bunsName %></p>
                <p><b>Bread Image:</b><br> <img src="/images/<%= buns.image %>" width="20%"></p>
                <p><b>Bread Type:</b><br> <%= buns.bunsType %></p>
                <p><b>Country of origin:</b><br> <%= buns.bunsCountry %></p>
                <p><b>Description:</b><br> <%= buns.bunsDesc %></p>
            </div>
        <% } else { %>
            <p>No bread found.</p>
        <% } %>
        <a href="/">Back</a>
    </div>
</body>
</html>
